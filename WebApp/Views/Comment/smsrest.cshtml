@using Twilio;
@{
    // set our AccountSid and AuthToken
    string AccountSid = "ACb42d92b735b2c5e9bd6f6a3bcec3d84e";
    string AuthToken = "76f6c47fea52b350c2a536aa4efeb277";

    // instantiate a new Twilio Rest Client
    var client = new TwilioRestClient(AccountSid, AuthToken);

    // make an associative array of people we know, indexed by phone number


    // iterate over all our friends


        // Send a new outgoing SMS by POSTing to the Messages resource */
        client.SendMessage(
            "+14695073806", // From number, must be an SMS-enabled Twilio number
            "+21654508528",     // To number, if using Sandbox see note above
                            // message content
            string.Format("Hey abbes, Monkey Party at 6PM. Bring Bananas!")
        );

       Response.Write(string.Format("Sent message to 0021627789707 "));
    
}