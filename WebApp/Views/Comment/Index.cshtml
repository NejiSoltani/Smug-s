@model IEnumerable<Data.Models.comment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.content)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.customer_idUser)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.idea_idIdea)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.manager_idUser)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.photo_idPhoto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.content)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.customer_idUser)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idea_idIdea)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.manager_idUser)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.photo_idPhoto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idComment }) |
            @Html.ActionLink("Details", "Details", new { id=item.idComment }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idComment })
        </td>
    </tr>
}

</table>
