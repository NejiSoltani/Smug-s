@model IEnumerable<Data.Models.idea>



@foreach (var item in Model)
{
    <li class="mt-list-item">
        <div class="list-icon-container">
            <a href="/Comment/ideacomment/@Html.DisplayFor(modelItem => item.idIdea)">
                <i class="fa fa-angle-right"></i>
            </a>
        </div>
        <div class="list-datetime bold uppercase font-green"> @Html.DisplayFor(modelItem => item.dateDepot) </div>
        <div class="list-item-content">
            <h3 class="uppercase">
                <a href="/Comment/ideacomment/@Html.DisplayFor(modelItem => item.idIdea)">@Html.DisplayFor(modelItem => item.subject)</a>
            </h3>
            <p>@Html.DisplayFor(modelItem => item.content)</p>
        </div>
    </li>
}


